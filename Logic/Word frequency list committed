from fastapi import APIRouter

router = APIRouter()

@router.get("/logic/unique-word-count")
def unique_word_count(input_text: str):

    words = input_text.split()
    unique_words = set(words)

    return {
        "input_text": input_text,
        "unique_word_count": len(unique_words)
    }
