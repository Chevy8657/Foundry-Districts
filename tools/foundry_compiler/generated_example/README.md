# Generated example plugin skeleton

This directory contains the output that the compiler will generate when run
against `example_definition.yaml`. It is included here to demonstrate the
expected output and to satisfy the acceptance test in the work order.
